#!/bin/sh
set -eu

project_root="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
cd "${project_root}"

if [ "${1:-}" = "--remove" ]; then
    exec docker compose down
fi

exec docker compose stop cyberlab
